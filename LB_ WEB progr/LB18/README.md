# Лабораторна робота №18: Масиви та функції

## Виконані завдання
1. **Робота з масивами:** Використано методи `map`, `filter`, `find` та `forEach` для обробки числових даних.
2. **Функції:** Створено стандартні функції, стрілкові функції та функції з параметрами за замовчуванням.
3. **Об'єкти:** Створено об'єкт `student` з методом `showInfo`, який використовує `this` для доступу до персональних даних.
